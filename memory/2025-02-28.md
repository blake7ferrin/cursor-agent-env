# 2025-02-28

- **Housecall Pro estimate for Tony Stark:** Built HCP integration (`bridge/housecall-pro.js`), fixed API paths (use `/customers`, `/estimates` without v1). Created estimate #63: 5-ton M-series split heat pump, standard attic, 45% margin ($10,763). Run `npm run create-estimate` in bridge/. Auth via `HCP_API_KEY`.
