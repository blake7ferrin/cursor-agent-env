# 2025-03-03

- Production hardening pass on bridge: async job mode (POST /chat?async=true, GET /jobs/:id), Zod validation for chat/changeout/estimate/export/housecall request, idempotency for Housecall export (Idempotency-Key), Redis for jobs + idempotency, structured logging (x-request-id, request_id/user_id/route/status/latency), DISABLE_HOUSECALL_REQUEST flag, new tests (jobs-store, idempotency-store, validation-schemas), README and PRODUCTION-HARDENING-SUMMARY.md updated. All 61 tests pass.
- Phase 6 MCP: bridge/mcp-server/ (schemas.js, tool-runner.js, stdio-server.js), GET /mcp/tools and POST /mcp/call when USE_MCP_TOOLS=true, docs/MCP-TOOLS.md, housecall.request gated by debug flag, tests mcp-tools.test.js and server-integration MCP tests. All 84 tests pass.
