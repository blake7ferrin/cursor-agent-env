# 2026-03-06

- Built Good/Better/Best 3-ton split heat pump options for Tony Stark from imported Day & Night catalog SKUs and computed pricing totals.
- Attempted live Housecall export via local bridge; Housecall API returned 404 for `/v1/customers` and `/v1/estimates` in this environment, so direct push could not complete here.
- Confirmed Housecall API in this account uses non-versioned paths (`/customers`, `/estimates`), updated bridge defaults/validation accordingly, and successfully created Tony Stark Good/Better/Best estimate (Housecall estimate_number 69) using customer_id `cus_552a549994fc4d02b19167bd516e3848`.
- Patched remaining stale docs/tests/comments to non-versioned Housecall endpoint examples (including MCP docs and README appointment lookup examples), and re-verified `/integrations/housecall/test` succeeds against `/customers` and `/estimates`.
- Implemented installer piece-rate labor automation in estimator flows: split/package base labor + common adders (weekend, tight attic, line-set, high-voltage, plenum) auto-attach via `labor_context`, and included `installer_pricing` metadata in estimate/export responses.
