# 2026-03-10

- Compared `main` with `origin/cursor/housecall-pro-heat-pump-options-dd51`: branch is ahead of main (no divergence), primarily adding installer piece-rate and Housecall path normalization.
- Created patch branch `cursor/housecall-pro-heat-pump-options-dd51-plus` from the heat-pump branch.
- Added mini-split base labor mapping in `bridge/installer-pricing.js` (`LABOR-BASIC-MINI-SPLIT-CHANGEOUT`).
- Added semantic overlap suppression in installer adders to reduce double-charging when equivalent complexity adders are already present.
- Extended `bridge/test/installer-pricing.test.js` with mini-split and overlap coverage; focused tests passed.
- Verified imported `preferred` catalog currently contains AC Pro equipment entries with populated costs (`unitCost`) used by estimator runtime.
