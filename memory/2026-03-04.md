# 2026-03-04

- Validated MCP availability path for Housecall/estimator workflows: Cursor MCP resource registry was empty in-session, but bridge stdio MCP server worked after fix.
- Fixed `bridge/mcp-server/stdio-server.js` so stdin data handler supports awaited requests and the process waits for in-flight async tool calls before exiting on stdin close.
- Added remote MCP JSON-RPC endpoint (`POST /mcp`) behind bridge auth to support registry-style remote MCP connections using `initialize`, `tools/list`, and `tools/call`.
- Added cloud rollout assets: `docs/CLOUD-MCP-HYBRID-SETUP.md` runbook + `bridge/scripts/smoke-mcp-registry.js` smoke test (`npm run test:mcp-registry`).
