# 2026-03-05

- Implemented MCP registry remote endpoint POST /mcp (initialize, tools/list, tools/call) with bridge auth + rate limiting; added integration tests, smoke script (npm run test:mcp-registry), and cloud hybrid setup docs.
- Installed Railway CLI, created project/service (cursor-agent-bridge/bridge), deployed bridge, synced Doppler secrets, generated Railway domain, set BRIDGE_PUBLIC_URL, verified /health and remote MCP smoke test.
- Evaluated Runlayer Cursor plugin fit/risk: strong governance features but fail-closed hooks and Runlayer-managed MCP policy could block custom MCP endpoints like current Railway bridge unless integrated with Runlayer.
- Updated global Cursor MCP config at C:\Users\blake\.cursor\mcp.json to add bridge-remote server pointing to Railway /mcp with Authorization from env BRIDGE_AUTH_TOKEN; set BRIDGE_AUTH_TOKEN user env var via Doppler.
- Verified Railway remote MCP server bridge-remote end-to-end: /health returned {"ok":true}; `npm run test:mcp-registry --prefix bridge` passed initialize/tools/list/tools/call against https://bridge-production-e91e.up.railway.app (auth via BRIDGE_AUTH_TOKEN env).
- Updated estimator preferred ingest source to new workbook `DayNight_ACPro_AI_Catalog.xlsx`, copied into `bridge/imports/incoming/`, and rebuilt catalog with `npm run ingest -- --profile preferred`.
- Enhanced `bridge/imports/ingest.js` XLSX parser to handle AI workbook headers (`SystemPrice`, `ModelNumber`, `SystemType`, `CondenserPrice`/`IndoorOrCoilPrice`/`FurnacePrice`) so rows are actually imported from the new catalog.
- Adjusted preferred profile `ignore_sheets` to skip `Catalog_DayNight_AI` and `Catalog_ACPro_AI`, ingesting `Catalog_All_AI` once to reduce duplicate imports.
